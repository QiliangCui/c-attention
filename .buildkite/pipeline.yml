steps:
  - label: ":python: Run Tests"    
    command:
      - "python3 -m venv venv"
      - "source venv/bin/activate"
      - "pip install -e .[test]"
      - "pip install pytest"
      - "pytest tests/"
